# Herda os pacotes de runtime da API para manter a paridade das versões de dependências
-r requirements.txt

# Framework de testes unitários automatizados e linter sintático de código
pytest==9.0.3
ruff==0.16.6

# Cliente HTTP para execução da suíte de testes de integração via FastAPI TestClient
httpx==0.28.1
