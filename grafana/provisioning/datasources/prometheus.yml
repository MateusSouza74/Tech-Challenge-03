apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus
    access: proxy
    # `prometheus` e o nome do servico no docker-compose. O Grafana resolve via
    # DNS interno do Docker; nao usar localhost, que seria o proprio container.
    url: http://prometheus:9090
    isDefault: true
    editable: false
